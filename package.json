{
    "name": "simplepub",
    "version": "1.0.0",
    "description": "Простой аналог Telegraph",
    "main": "server.js",
    "scripts": {
      "start": "node server.js",
      "dev": "nodemon server.js",
      "build": "mkdir -p dist && cp server.js dist/",
      "test": "echo \"Error: no test specified\" && exit 1"
    },
    "engines": {
      "node": ">=18.0.0"
    },
    "dependencies": {
      "compression": "^1.7.4",
      "cors": "^2.8.5",
      "express": "^4.18.2",
      "helmet": "^7.0.0",
      "pg": "^8.11.0"
    },
    "devDependencies": {
      "nodemon": "^3.0.1"
    }
  }